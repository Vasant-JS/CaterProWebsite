# CaterPro Website

Static landing page for CaterPro, ready to deploy on Render.

## Render

This repository includes `render.yaml` for Render Blueprint deployment.

- Service type: static site
- Publish path: repository root
- Entry page: `index.html`
